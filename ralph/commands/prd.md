---
description: Generate a Product Requirements Document (PRD) for a new feature
argument-hint: Feature description (e.g., "user notifications")
allowed-tools: ["Read", "Glob", "Grep", "Write", "Bash", "AskUserQuestion", "Skill"]
---

# PRD Generator

Generate a PRD for the feature described in the arguments (or ask the user if not provided).

**Load the prd skill first** using the Skill tool, then follow its workflow exactly.

**Initial request:** $ARGUMENTS
